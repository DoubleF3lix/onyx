# Used to keep the Commands class unbound from the pack object. Instead of storing the existing pack object (used for Execute), we store it here so it can be access independently.
# cpo stands for Current Pack Object
cpo = None
